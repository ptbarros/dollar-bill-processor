# CUDA edition dependencies (NVIDIA GPU: torch + EasyOCR both on CUDA).
# torch/torchvision are installed SEPARATELY in CI from the cu128 index
# (pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128)
# so they are intentionally NOT listed here.
#
# This is the "fast on a good NVIDIA GPU" build. The default torch-free build
# uses requirements.txt (onnxruntime + RapidOCR) instead.

opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
pyyaml>=6.0
PySide6>=6.5.0
pandas>=2.0.0
openpyxl>=3.1.0
python-docx>=1.0.0
lupa>=2.0

# AI pattern generation (bundled so the AI tab works in the frozen app):
anthropic>=0.40
openai>=1.50

# GPU inference stack (torch supplied via the cu128 index in CI):
ultralytics>=8.0.0
easyocr>=1.7.0
